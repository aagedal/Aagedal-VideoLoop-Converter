<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
    <channel>
        <title>Aagedal Media Converter</title>
        <link>https://github.com/aagedal/Aagedal-Media-Converter</link>
        <description>Most recent changes for Aagedal Media Converter.</description>
        <language>en</language>

        <!--
            Each release adds an <item> below. Replace the example fields with real values
            produced by scripts/release.sh, which runs Sparkle's `sign_update` tool.

            Required fields per item:
              - <title>             — human-readable release name
              - <pubDate>           — RFC 822 date (the `date -R` output on Linux, or
                                      `date "+%a, %d %b %Y %H:%M:%S %z"` on macOS)
              - <sparkle:version>   — CFBundleVersion (build number, e.g. 520)
              - <sparkle:shortVersionString> — CFBundleShortVersionString (e.g. 4.1.2)
              - <sparkle:minimumSystemVersion> — macOS minimum (15.0)
              - <enclosure>:
                  url           — download URL of the signed .zip
                  length        — file size in bytes
                  type          — application/octet-stream
                  sparkle:edSignature — EdDSA signature from `sign_update`
              - <description>       — release notes (CDATA-wrapped HTML)
        -->

        <!-- Example placeholder (delete once the first real release ships):
        <item>
            <title>Version 4.1.2</title>
            <pubDate>Mon, 11 May 2026 12:00:00 +0000</pubDate>
            <sparkle:version>520</sparkle:version>
            <sparkle:shortVersionString>4.1.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://codeberg.org/taagedal/Aagedal-Media-Converter/releases/download/4.1.2/Aagedal_Media_Converter_4-1-2.zip"
                length="0"
                type="application/octet-stream"
                sparkle:edSignature="REPLACE_WITH_OUTPUT_OF_sign_update" />
            <description><![CDATA[
                <ul>
                    <li>First Sparkle-enabled release.</li>
                </ul>
            ]]></description>
        </item>
        -->
        <item>
            <title>Version 4.1.2</title>
            <pubDate>Mon, 11 May 2026 22:52:03 +0200</pubDate>
            <sparkle:version>520</sparkle:version>
            <sparkle:shortVersionString>4.1.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://codeberg.org/taagedal/Aagedal-Media-Converter/releases/download/4.1.2/Aagedal_Media_Converter_4-1-2.zip"
                length="100196871"
                type="application/octet-stream"
                sparkle:edSignature="juMtv5737GuJrhT7b4ZqpY5QjFFqmODBX9ahsxuOoSOmT34j6eEAC4u14XEsgCaWiiJbsjbyI9KJosdiCj2XCg==" />
            <description><![CDATA[
                <p>The headline is auto-update support for direct-download installs and a move of the project repository from GitHub to Codeberg. Also rolls up the small fixes that landed since 4.1.0 was tagged.</p>
                <h2>Auto-update</h2>
                <ul>
                <li><strong>Direct-download installs now auto-update.</strong> A new updater (built on the Sparkle framework) checks for new releases on a configurable schedule, downloads them in the background, and — if you've opted in — installs the update on next launch. Daily / weekly / monthly cadence and an <strong>Install updates automatically</strong> toggle live under <strong>Settings → Updates</strong>, alongside a <strong>Check for Updates…</strong> item that's also reachable from the app menu. Update binaries are EdDSA-signed against a key embedded in this release; tampered or downgrade attempts are rejected before install.</li>
                <li><strong>Homebrew installs are detected and routed through <code>brew upgrade</code> instead.</strong> The auto-updater never replaces a bundle that brew manages — that would create a checksum mismatch on the next <code>brew upgrade --cask</code>. When the in-app check finds an update on a brew install, the notification offers a one-click <strong>Copy brew Command</strong> button (<code>brew upgrade --cask aagedal-media-converter</code>) in place of the Download button, and Settings → Updates shows the same command with a copy-to-clipboard helper.</li>
                </ul>
                <h2>Moved to Codeberg</h2>
                <ul>
                <li><strong>Source code is available at <a href="https://github.com/aagedal/Aagedal-Media-Converter">github.com/aagedal/Aagedal-Media-Converter</a>.</strong></li>
                <li><strong>The Homebrew tap is available at <a href="https://github.com/aagedal/homebrew-tap">github.com/aagedal/homebrew-tap</a>.</strong> If you used the temporary Codeberg tap, switch back once: <code>brew untap taagedal/tap &amp;&amp; brew tap aagedal/tap</code>.</li>
                <li><strong>The in-app update check now polls Codeberg's releases API</strong>, so existing 4.1.1 installs surface this release in the update banner and can manually download it. After installing 4.1.2, Sparkle takes over for direct-download users; brew users continue to get notified by the in-app check and routed to brew.</li>
                </ul>
                <h2>Bug fixes (rolled up from 4.1.1 and 4.1.2)</h2>
                <ul>
                <li><strong>Capture region overlay can now be set to pass mouse clicks through</strong> to the windows underneath, so the recording region selector doesn't block interaction with whatever's behind it while you frame a shot.</li>
                <li><strong>Upload settings now persist the full password.</strong> A truncation bug was storing a shortened version, which broke authentication after relaunch. Existing entries continue to work — just re-enter the password if upload fails.</li>
                <li><strong>Tab key in the Upload and General settings panes</strong> is no longer captured by the queue's table view. Settings now tracks its own NSWindow via <code>viewDidMoveToWindow</code>, so Tab cycles between fields the way it should.</li>
                <li><strong>Merged-group upload counter</strong> showed "1/N" instead of "1/1" when uploading a merged group — fixed.</li>
                <li><strong>README screenshots scale correctly on Codeberg.</strong> The inline <code>width</code>/<code>height</code> attributes that GitHub respects were being mis-applied by Codeberg's renderer, leaving the images either huge or tiny. Letting them flow at their natural size fixes the layout.</li>
                </ul>
            ]]></description>
        </item>
        <item>
            <title>Version 4.1.3</title>
            <pubDate>Wed, 13 May 2026 14:10:45 +0200</pubDate>
            <sparkle:version>521</sparkle:version>
            <sparkle:shortVersionString>4.1.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://codeberg.org/taagedal/Aagedal-Media-Converter/releases/download/4.1.3/Aagedal_Media_Converter_4-1-3.zip"
                length="100250703"
                type="application/octet-stream"
                sparkle:edSignature="3O8ginUY4Hxo855cLOjenIHX7YIhSxYGBlQRGIxd411P4/xxoi4wOKSeGaOsJUMWiZ9MJrFJAGz9K4u2vjWFCw==" />
            <description><![CDATA[
                <p>A focused follow-up to 4.1.2. The recording-region picker now feels native — aspect-ratio lock plus macOS-style Shift / Option / Space modifiers during a drag — camera-card imports gained an opt-in "start encoding after import" toggle and finally do the right thing on multi-format cards, deleting an encoding group mid-upload no longer crashes, and new installs are opted in to automatic updates with a friendly first-launch opt-out notice.</p>
                <h2>Recording region</h2>
                <ul>
                <li><strong>Aspect-ratio lock for the capture region.</strong> A new menu in the floating control panel offers <strong>16:9</strong>, <strong>9:16</strong>, <strong>1:1</strong>, <strong>3:2</strong>, <strong>2:3</strong>, <strong>4:3</strong>, and <strong>3:4</strong> (persisted per app launch). When a ratio is locked, the rectangle snaps to it while you draw and resize, so you don't have to eyeball the proportions.</li>
                <li><strong>macOS-native modifier keys during a region drag.</strong> Hold <strong>Space</strong> to move the rectangle rigidly while drawing, then release to resume drawing from the new anchor. Hold <strong>Shift</strong> to lock the non-dominant axis (aspect-ratio takes precedence when both apply). Hold <strong>Option</strong> to scale symmetrically from the start point when drawing, or from the rect center when resizing. Modifier transitions replay the last drag value, so the region updates the moment you press or release a key — no extra mouse motion required.</li>
                <li><strong>Shift mid-drag now preserves the size you already drew</strong> instead of collapsing the locked axis to a 1pt line. Holding Shift <em>before</em> the drag starts still produces the perfectly horizontal or vertical guide as before.</li>
                <li><strong>The capture overlay now holds keyboard focus</strong> while the app is hidden, so Shift / Option / Space actually reach the drag pipeline. The previous build's <code>nonactivatingPanel</code> + <code>NSApp.hide</code> combo sent keystrokes to whichever app was previously frontmost, which is why modifiers silently did nothing during a drag. Space is now swallowed by the overlay too, instead of leaking through to whatever's playing underneath.</li>
                </ul>
                <h2>Camera-card imports</h2>
                <ul>
                <li><strong>"Start encoding after import" toggle in the camera-card sheet</strong> (persisted across launches). Honoured by the regular, auto-split, and force-merge import paths — so multi-clip imports start encoding the moment they hit the queue when the option is on.</li>
                <li><strong>Multi-format card imports now honour the concat + upload intent.</strong> When clips on the same card can't merge into one file (e.g. the operator switched to 4K or slow-mo mid-shoot), plain <strong>Import</strong> is no longer the primary action — <strong>Auto-split</strong> takes over with a tooltip pointing at the concat toggle, so Return picks the safe path that actually preserves your intent. Per-group filenames now include codec in the suffix, with <code>_g2</code>/<code>_g3</code>/… tie-breakers if any names still collide. Previously, H.264 1080p25 + H.265 1080p25 on the same card produced two outputs with the same base name.</li>
                </ul>
                <h2>Stability</h2>
                <ul>
                <li><strong>Deleting an encoding group while it's uploading no longer crashes.</strong> Removing a group with an active rclone upload used to crash with "Index out of range" — the deletion handler tore down the group's array slot while the upload kept running and its progress callback fired into a binding whose backing array had just shrunk. The handler now cancels every in-flight upload in the group via <code>UploadManager.cancelUpload(itemID:)</code> first, and the upload-progress callback bounds-checks against a snapshot of the binding so any late-arriving rclone tick for a removed item is dropped silently.</li>
                <li><strong>New per-group Cancel upload button</strong> on the group header's upload row gives you a non-destructive way to stop an upload — you no longer have to delete the whole group to abort. Visible only while the group is actively uploading or pending upload; hidden once the upload settles.</li>
                </ul>
                <h2>Auto-update</h2>
                <ul>
                <li><strong>New installs are opted in to automatic updates</strong> by default. Sparkle's update dialog doesn't surface the auto-install preference, so users would otherwise have to dig into Settings to enable it. A one-time first-launch notice ("Automatic updates are on … turn off in Settings → Updates") with an <strong>Open Settings…</strong> shortcut lets you flip it back if you'd rather review each update by hand.</li>
                <li><strong>The first-launch notice is suppressed on Homebrew installs</strong> (brew owns the bundle there), and is hardened against drift: the "have I shown this yet" flag survives across app updates via <code>UserDefaults.standard</code>, and the notice only fires when Sparkle is actually configured to install updates automatically — so a user who explicitly turned the preference off doesn't see a notice claiming otherwise.</li>
                </ul>
                <h2>Localization</h2>
                <ul>
                <li><strong>Norwegian translations caught up</strong> across recent additions: Settings sidebar tabs (the labels were plain <code>String</code>s, so <code>Label.init</code> bypassed catalog lookup), FileZilla import, IMF / DCP wrap errors, MCA default labels, custom-filename template, capture overlay, the Sparkle / Homebrew update flow, and various settings and help text.</li>
                <li><strong>Typo and half-translation fixes</strong> — "Vesjon" → "Versjon", "S3-kompatibel Storage" → "S3-kompatibel lagring", and positional placeholder fixes for the plural <code>%lld clip%@</code> forms.</li>
                </ul>
                <h2>Housekeeping</h2>
                <ul>
                <li>Drops the <code>com.apple.security.network.client</code> entitlement from both Debug and Release builds. It's a sandbox-only restriction; under the hardened runtime without the sandbox, it does nothing. The <code>com.apple.security.cs.disable-library-validation</code> entitlement stays — still required to load the bundled FFmpeg / MPV / VLCKit dylibs. No behavioural change for users.</li>
                </ul>
            ]]></description>
        </item>
        <item>
            <title>Version 4.2.0</title>
            <pubDate>Mon, 08 Jun 2026 09:12:51 +0200</pubDate>
            <sparkle:version>572</sparkle:version>
            <sparkle:shortVersionString>4.2.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://aagedal.me/apps/Aagedal_Media_Converter_4-2-0.zip"
                length="110620602"
                type="application/octet-stream"
                sparkle:edSignature="Gi4aLfLTKAYMD68iGeqvnuPNC8Zzo2SWj1zf41oZypDk4f9AGR4WRV2Nb6ZEkom5McqT53mcPlt0YvHWZ4uaBw==" />
            <description><![CDATA[
                <p>The headline is experimental <strong>AV2 encoding</strong> via the bundled AOM AVM reference encoder — full round-trip (encode, decode, thumbnails, preview-not-available messaging), parallel chunked encoding that actually uses your cores, and Matroska muxing so the bitstream lands in a real container. Also new: <strong>growing-file screen recording</strong> — record straight into DaVinci Resolve (and Premiere) and edit the clip while it's still being captured — now across <strong>several displays at once</strong> in a tiled live overlay, plus a redesigned recording menu-bar menu, an expandable capture overlay, and a new <strong>virtual display</strong> you can record a window onto off-screen, without it cluttering your real monitors. Alongside: opt-in <strong>settings &amp; custom-preset sync</strong> across Macs, a much faster queue sort, and a handful of crash and correctness fixes.</p>
                <h2>Screen recording — growing files</h2>
                <ul>
                <li><strong>Three growing presets, now the default for screen recording:</strong> <em>Growing HEVC 10-bit 4:2:2 (Resolve/Premiere)</em> (the default), <em>Growing H.264 (Compatibility)</em>, and <em>Growing ProRes 4444 (Visually Lossless)</em>. Each writes a fragmented <code>.mov</code> in-process via AVAssetWriter (hardware HEVC 10-bit 4:2:2, H.264 for maximum compatibility, or visually-lossless ProRes 4444), so the file is editable the moment recording starts and survives an interruption. Non-growing HEVC 10-bit 4:2:2 and ProRes 4444 presets remain for when you'd rather import after recording; the Settings preset picker now spells out the trade-off (constant frame rate / edit-while-recording vs variable frame rate / smaller files). Note ProRes can't compress duplicate frames, so growing ProRes on a mostly-static screen makes large files.</li>
                <li><strong>Recognised as a true growing file by DaVinci Resolve</strong> — the recording is tagged with the Blackmagic <code>com.blackmagicdesign.metadata:recording</code> extended attribute while capturing (reverse-engineered; see <code>docs/growing-file-research</code>), so Resolve shows the red REC overlay and fast-refreshes the clip (~5 s) instead of its generic ~1-min media rescan. The attribute is removed automatically on stop — and on a write error, or swept on next launch if a crash or quit interrupted a recording — so a finished or abandoned clip never carries a stale REC overlay.</li>
                <li><strong>Constant frame rate + timecode.</strong> ScreenCaptureKit delivers frames only on change; a CFR pump re-emits the latest frame on a fixed clock so the file has a clean constant frame rate (important for NLEs), and a per-frame timecode track is written starting at wall-clock time-of-day.</li>
                <li><strong>Removed the broken growing-<code>.ts</code> path.</strong> The experimental FFmpeg-pipe <code>.ts</code> presets (which deadlocked — HEVC produced a 0 KB file, H.264 froze after one frame) are gone, along with their dead pipe writer. Stored defaults pointing at them fall back to a working preset automatically.</li>
                </ul>
                <h2>Screen recording — overlay controls</h2>
                <ul>
                <li><strong>One menu-bar menu instead of two icons.</strong> While recording, the menu bar previously showed two separate icons — a red stop button and a rectangle to re-open the hidden preview. They're now a single red icon whose dropdown holds everything: <strong>Stop Recording</strong>, <strong>Show / Hide Preview</strong>, an <strong>Extend Auto-Stop</strong> submenu (<strong>+1 / +5 / +10 / +30 min</strong>, plus <strong>Cancel Auto-Stop</strong>), and a live status section showing how long you've recorded and — when an auto-stop is set — how long is left and the exact stop time. Extending when no auto-stop is active simply starts one from now.</li>
                <li><strong>Expandable capture overlay.</strong> The floating preview panel toggles between a compact size and an expanded view via the button in its top-right corner. Expanded fills ~90% of its display, sized to the captured area's aspect ratio so the preview fills edge-to-edge with no letterboxing — useful for keeping an eye on the capture from across the room, on an external display, or while navigating a virtual screen. The audio meters keep their width (only their height grows with the video preview), the panel stays clamped on-screen, and your choice is remembered across launches. The live preview's capture resolution scales up with the panel so it stays sharp.</li>
                </ul>
                <h2>Screen recording — multiple displays</h2>
                <ul>
                <li><strong>Record several screens at once, each to its own file.</strong> The display picker in the floating overlay is now multi-select (with a <strong>Record All Displays</strong> shortcut), and every selected screen records in parallel to its own <code>.mov</code> named <code>ScreenCapture_&lt;timestamp&gt;_&lt;DisplayName&gt;.mov</code> so files never collide. Each file is fully self-contained — it gets the system-audio and (when enabled) microphone tracks plus the growing-file treatment (Blackmagic REC attribute, constant frame rate, per-frame timecode) — so every screen is independently importable as its own multicam angle, all sharing one start timestamp.</li>
                <li><strong>A single tiled overlay, not one panel per screen.</strong> The preview shows a live tile per selected display, laid out side-by-side for two and wrapping into a grid for three or more, with the audio meters flanking the whole grid. The panel sizes itself to the tiles and still toggles between compact and expanded.</li>
                <li><strong>Per-screen controls on hover.</strong> Hovering a tile reveals three buttons: <strong>expand</strong> that screen to a ~90% focus view (and collapse back to the grid), <strong>start / stop recording</strong> for that screen alone, and <strong>remove</strong> it from the recording. Recording is fully independent — you can start one screen, add another mid-session, stop or remove a screen while the rest keep rolling; a red <strong>REC</strong> badge marks the tiles that are live. Stopping the last active screen ends the session and offers all the saved files at once (reveal-all in Finder, or add them all to the encoding queue).</li>
                <li><strong>Region capture stays single-display.</strong> Drawing a capture region still targets one screen; switching to Region mode collapses a multi-screen selection down to the primary display.</li>
                </ul>
                <h2>Screen recording — virtual display</h2>
                <ul>
                <li><strong>Record a window off-screen on a virtual display.</strong> A new ＋ menu beside the display picker — in both the floating control panel and the capture window — spins up a headless <strong>virtual display</strong> (a real macOS display backed by no hardware) at <strong>720p, 1080p, 1440p, or 4K</strong>. Drag any window or live feed onto it and record it like any monitor, without it taking up space on your physical screens. The new display appears in the display picker automatically and is selected for capture straight away; a <strong>Remove</strong> entry in the same menu tears it down, and any virtual displays are cleaned up on quit so none linger.</li>
                <li><strong>Created as an extended desktop, not a mirror.</strong> macOS drops a freshly created virtual display into a mirror set, pinned on top of your main screen. Each one is immediately detached into the extended desktop — session-scoped, so it never touches your saved display arrangement — so what you record is its own independent space rather than a copy of your main monitor.</li>
                <li><strong>Pixel-perfect capture.</strong> The display is a plain SDR panel at exactly the chosen resolution, so the recording is those precise pixels with no scaling.</li>
                <li><strong>Built from scratch, GPL-clean.</strong> Virtual displays use Apple's private <code>CGVirtualDisplay</code> API — the same mechanism apps like BetterDisplay rely on, since there's no public API for this. The interface is hand-declared in this project (no third-party code), so it stays fully open-source and GPL-3.0, adds no dependencies, and is isolated behind a runtime availability check that degrades to "unavailable" if the private API ever changes — leaving normal display capture unaffected. As a private API it isn't App-Store-compatible, which doesn't affect this direct-download build.</li>
                </ul>
                <h2>AV2 encoding (experimental)</h2>
                <blockquote>⚠️ <strong>Experimental.</strong> AV2 isn't in FFmpeg yet, so encoding goes through the AOM AVM reference encoder, which is inherently very slow (tens of seconds per frame at HD even at the fastest preset). Output is a young, evolving bitstream — treat it as a preview, not a delivery format.</blockquote>
                <ul>
                <li><strong>New <code>.av2</code> export preset.</strong> Encoded by the bundled <code>avmenc</code> over a two-process pipe: FFmpeg decodes / trims / scales to Y4M, piped into <code>avmenc</code>. Supports Constant-Quality (<code>--qp</code>) and VBR (<code>--target-bitrate</code>) modes, 8/10-bit, speed, and tiling, with an <strong>EXPERIMENTAL</strong> badge and a video-only warning in the AV2 settings card.</li>
                <li><strong>Parallel chunked encoding.</strong> The clip is split into one frame-range chunk per core and a separate <code>avmenc</code> runs per chunk, then the segment <code>.ivf</code> files are joined in Swift. This scales far better than AVM's tile threading and compresses better (no tile boundaries). Auto-on, with a single-process escape hatch; Constant-Quality only (VBR can't span chunk boundaries). A configurable chunk count lives in AV2 settings.</li>
                <li><strong>Auto-tiling for single-process encodes.</strong> AVM only parallelizes across tiles, so an untiled encode runs effectively single-threaded. When tiling is left on <strong>Auto (0)</strong>, tile columns/rows are now derived from the frame size (keeping each tile ≥ ~256px) — measured ~30% faster on an 18-core machine. Explicit tile values are still respected. Default speed is now the fastest <code>avmenc</code> setting (cpu-used 9).</li>
                <li><strong>Matroska muxing.</strong> FFmpeg can't write AV2, so a new in-app muxer writes a <code>.mkv</code> with a <code>V_AV2</code> video track plus audio (AAC in ADTS, or Opus in Ogg with proper <code>OpusHead</code> / CodecDelay / SeekPreRoll). The AV2 CodecPrivate is harvested from a one-frame <code>avmenc --webm</code> probe; video frames are copied verbatim from the IVF. New AV2 settings choose the container (IVF / Matroska) and audio codec + bitrate.</li>
                <li><strong>Full decode round-trip.</strong> <code>.ivf</code> and AV2-in-Matroska (<code>.mkv</code> / <code>.webm</code>) sources now decode through <code>avmdec</code> → FFmpeg, so AV2 is a real input, not encode-only. Decoding goes via self-describing <strong>Y4M</strong>, so 4:2:0 / 4:2:2 / 4:4:4 and 8/10-bit are all preserved instead of being silently downsampled to 4:2:0. Verified: AV2 <code>.mkv</code> → HEVC and ProRes 422 with audio intact.</li>
                <li><strong>Queue rows and thumbnails for AV2.</strong> <code>.ivf</code> files are classified as video (dimensions and duration read from the IVF header in pure Swift, since FFmpeg/SwiftExif can't probe AV2), so they show a real duration and a decoded thumbnail and route correctly when transcoded to another video preset. The interactive trim player shows a clear "Preview not available for this format" message — there's no AV2 decoder in AVFoundation / VLCKit / MPV yet.</li>
                <li><strong>Accurate encode progress.</strong> Progress is now driven by <code>avmenc</code>'s per-frame <code>POC:</code> output rather than FFmpeg (which only decodes the source and finishes in seconds), so the bar tracks the real encode instead of pinning at 100% while <code>avmenc</code> is still working.</li>
                <li><strong><code>avmenc</code> / <code>avmdec</code> are codesigned</strong> with the Developer ID Application identity, hardened runtime, and a secure timestamp — matching the other bundled CLI tools so notarization passes.</li>
                </ul>
                <h2>Settings &amp; custom-preset sync</h2>
                <ul>
                <li><strong>Sync an allowlisted slice of settings across Macs</strong> — your 10 custom presets, codec settings, file-naming, waveform, and general preferences — as a single versioned JSON snapshot. Off by default.</li>
                <li>Because this is a non-App-Store build with no iCloud entitlement, <strong>"iCloud sync" writes the snapshot into the iCloud Drive folder on disk</strong> (CloudDocs), which the OS syncs automatically. The same format also powers a <strong>custom folder</strong> target and manual <strong>Export / Import</strong> from the File menu.</li>
                <li>Machine-specific data (folder paths, bookmarks, binary install state, upload credentials) is deliberately excluded from the allowlist. A rolling local backup is taken before every apply, with newest-wins-and-notify, loop guards, and graceful handling when iCloud Drive is off. New <strong>Sync</strong> settings tab; an "updated from &lt;Mac&gt;" toast confirms an incoming change.</li>
                </ul>
                <h2>Performance</h2>
                <ul>
                <li><strong>Much faster queue sort with many items.</strong> Sorting resolved each item's sort key lazily inside the comparator, so every one of the O(n log n) comparisons did an O(n) scan <em>and</em> a synchronous disk stat for the creation date — one keypress fired thousands of blocking <code>stat</code> calls on the main thread (part of the "lagging with many queued items" symptom). Keys are now resolved once up front (each file stat'd at most once, and only when the sort mode needs dates), then sorted on cached keys. The group-editor sort got the same decorate-sort-undecorate treatment.</li>
                <li><strong>Row-thumbnail generation is decoupled from encode start.</strong> Starting an encode used to block on metadata probing <em>and</em> row-thumbnail generation together; for non-native card formats (MKV/MXF/MTS) the thumbnail is an FFmpeg seek+decode that can take several seconds, stalling conversion even though the encode never uses the thumbnail. Encode start now waits only for the metadata it needs; the thumbnail is generated independently on the import task.</li>
                </ul>
                <h2>Uploads</h2>
                <ul>
                <li><strong>Force-retry failed group uploads with Option-click.</strong> Plain-clicking a group header's upload icon still toggles upload on/off; <strong>Option-clicking</strong> now restarts every item in the group whose upload failed or was cancelled, mirroring the existing per-item modifier-click.</li>
                </ul>
                <h2>Stability</h2>
                <ul>
                <li><strong>No more crash on Macs with zero active displays.</strong> Four screen-capture sites used <code>NSScreen.main ?? NSScreen.screens[0]</code>, which traps when no display is active (<code>NSScreen.main</code> can be nil <em>and</em> <code>screens</code> can be empty). They now use <code>.screens.first</code> with a guard.</li>
                <li><strong>Custom-command parsing no longer drops an empty quoted argument.</strong> An explicitly empty quoted arg (e.g. <code>-vf &quot;&quot;</code>) used to vanish, shifting every following token onto the wrong flag. It's now emitted correctly as <code>&quot;&quot;</code>.</li>
                </ul>
                <h2>Housekeeping</h2>
                <ul>
                <li><strong>SwiftExif updated to 1.9.1.</strong> Both consumers compile unchanged.</li>
                <li><strong>Release zips strip AppleDouble metadata</strong> (<code>--norsrc --noextattr --noacl --noqtn</code>). Without this, <code>ditto</code> encodes xattrs / ACLs / creation dates as <code>._&lt;name&gt;</code> companions inside the zip, which macOS Sequoia no longer merges back on extract — they surface as visible files inside the <code>.app</code>, break the codesignature seal, and trip Gatekeeper's "app is damaged". The 4.1.2 release zip was re-packaged retroactively with the same fix.</li>
                <li>Removed the stale <code>GEMINI.md</code> project-context file (recoverable from git history).</li>
                </ul>
            ]]></description>
        </item>
        <item>
            <title>Version 4.3.0</title>
            <pubDate>Sat, 27 Jun 2026 17:58:31 +0200</pubDate>
            <sparkle:version>575</sparkle:version>
            <sparkle:shortVersionString>4.3.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/aagedal/Aagedal-Media-Converter/releases/download/4.3.0/Aagedal_Media_Converter_4-3-0.zip"
                length="110114696"
                type="application/octet-stream"
                sparkle:edSignature="fVdi53M3DREHoSsCM+9l/GXQ7AuiPHxesljjohp/DXqTOIrCCnXwUsjvk41o3v4Z/s59SN/rrpYXa8/rgHu2Aw==" />
            <description><![CDATA[
                <p>A smaller, focused release built around <strong>Shortcuts &amp; Spotlight</strong>. Instead of a single "convert with whatever's selected" action, there's now <strong>one Convert action per export preset</strong>, a <strong>Convert with Default Preset</strong> action that follows your configured default — so it can drive any of your <em>custom</em> presets — and the most common presets are surfaced as zero-setup <strong>Spotlight / Siri shortcuts</strong>. All of them now <strong>launch the app automatically</strong> when it isn't already running. Under the hood, the bundled <strong>FFmpeg moves to 8.1.1</strong>. Rounding it out: the queue's <strong>drag-to-share handle now works on every drag</strong> (not just the first after launch), and <strong>importing from a camera card no longer freezes the window</strong>.</p>
                <h2>Shortcuts &amp; App Intents</h2>
                <ul>
                <li><strong>One "Convert Immediately" action per built-in preset.</strong> Every built-in export preset — VideoLoop, VideoLoop with Sound, Animated Still, H.264, H.265, AV1, AV2, TV (HEVC and AVC-Intra), ProRes, Proxy, Stream Copy, Audio Only, Image Sequence, DCP, and both IMF flavours — is now its own Shortcuts action (e.g. <em>Convert to ProRes</em>, <em>Extract Audio</em>, <em>Remux (Stream Copy)</em>). Each adds the files, sets the output folder to the source folder, switches the app to that preset, and starts conversion. Actions use your saved settings for that preset (encoder, resolution, container, quality, audio), so a one-tap shortcut behaves exactly like picking the preset in the app.</li>
                <li><strong>New "Convert with Default Preset" action.</strong> Converts using whatever you've set as the default preset in Settings, resolved at the moment it runs. Because the default can be <em>any</em> preset — including one of the ten user-defined <strong>Custom</strong> slots — this is the way to drive a custom preset from Shortcuts or Spotlight, which can't expose each custom slot as its own static action (their names are user-defined).</li>
                <li><strong>Spotlight &amp; Siri shortcuts.</strong> The ten most common presets — led by <strong>Convert (Default Preset)</strong> — are exposed as zero-setup App Shortcuts, so you can run them from Spotlight or by voice (e.g. <em>"Convert with Aagedal Media Converter"</em>) without first building a shortcut. Because a Spotlight/Siri phrase can't carry files, running one this way opens the app, switches to that preset, and presents a file picker — convert actions then start as soon as you choose your files. Apple caps this surface at ten per app; the niche presets (AV2, TV, Image Sequence, DCP, IMF) stay available as full actions inside the Shortcuts app.</li>
                <li><strong>The app now opens automatically.</strong> Every convert action and "Add to Encode Queue" now launches the app if it isn't already running and reliably hands off its files, instead of silently doing nothing when the app was closed. A small launch-time buffer replays a request that arrives before the window's receivers are ready, so nothing is lost to the cold-launch race — and never runs twice.</li>
                </ul>
                <h2>Encoding</h2>
                <ul>
                <li><strong>Bundled FFmpeg updated to 8.1.1.</strong> Replaces the previous build that dynamically linked Homebrew's <code>libvorbis.dylib</code> — and crashed on launch — with a self-contained, statically-linked GPL build. Non-free components are dropped: <code>libfdk_aac</code> gives way to the native <code>aac</code> / <code>aac_at</code> encoders.</li>
                </ul>
                <h2>Fixes</h2>
                <ul>
                <li><strong>Drag-to-share works on every drag, not just the first.</strong> The drag handle on a finished queue row (the four-arrows icon) lets you drag the exported file straight into another app or Finder. It used to work only on the first drag after launch and then go dead — or start reordering the queue instead — because the drag was started from the wrong place and an unfinished drag session blocked all the ones after it. It now begins a proper file drag every time. The drag image is the row's own thumbnail (rounded, with a soft drop shadow) instead of a generic black document icon, and dropping the file back onto the queue re-adds it so you can compare the export against the original.</li>
                <li><strong>Importing from a camera card no longer freezes the window.</strong> Scanning a camera/SD card ran on the main thread, locking up the UI until it finished. The scan now runs off the main actor, so the window stays responsive.</li>
                </ul>
            ]]></description>
        </item>
    </channel>
</rss>
